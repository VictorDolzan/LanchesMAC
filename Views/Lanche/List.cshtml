@model IEnumerable<LanchesMAC.Models.Lanche>

@foreach(var lanche in Model)
{
    <div>
        <h4>@lanche.Name</h4>
        <p><img src="@lanche.ImagemUrl" style="max-width: 200px;"></p>
        <h3>Preço : @lanche.Preco.ToString("c")</h3>
    </div>
}